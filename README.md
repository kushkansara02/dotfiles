# Getting started

1. Clone this repository under `Projects/`.
2. Run `bootstrap/bootstrap.sh` to install ALL necessary dependencies.
3. (Optional) run `make configuration` if some configuration already exists
   - Warning: you will lose your changes
4. Run `make` to establish symlinks to your root directory
